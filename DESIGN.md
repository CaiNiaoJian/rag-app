# DESIGN.md —— DocFactory 设计系统

> 单一事实来源是 `app/src/renderer/src/styles.css` 的 `:root`。本文解释**为什么**是这些值。
> 与 07 章《UI 设计》配合：07 章定信息架构与文案，本文定视觉语言。

## register：product

design **serves** the product。用户在任务里，工具应当消失在任务背后。
成功标准不是「看起来像 AI 做不出来的」，而是：一个熟悉 Linear / Notion / Claude 的人
坐下来就信任这个界面，不会在每个组件前停顿一下觉得哪里怪。

**失败模式不是平淡，是无目的的古怪**——过度装饰的按钮、样式不统一的表单控件、
给标准任务发明新控件。这里的标准是 earned familiarity。

## 主题：浅色为主 + 可切深色

> 内网知识管理员，白天在办公室工位、1080p 屏前，把几百份历史文档丢进来批量整理，
> 一边等解析一边在三栏对照里核对表格有没有丢行。

这句话决定了**浅色是默认主场**：文档本身是白底黑字，「原文 vs Markdown」对照时
深色 UI 会造成亮度跳变。但主题是用户偏好而非教条——设置页提供
跟随系统 / 浅色 / 深色 三档（存 localStorage，`<html data-theme>` 生效）。
深色是**暖深褐**（Claude 式）而非冷黑：与浅色同一套色相体系，只翻转明度。

## 色彩：暖纸感中性阶 + 赤陶橙单强调（Anthropic 式）

品牌色是**赤陶橙**（OKLCH 色相 42，terracotta），中性阶带**暖黄褐色相**（hue 80，
chroma 0.004–0.012）——整屏像同一张纸，而不是「灰底上放了个橙按钮」。

- 没有 `#fff` / `#000`：纯白在大面积上发涩，纯黑文字在浅底上过硬。文字是暖黑褐。
- 状态色（橄榄绿 / 琥珀 / 赤红）彩度压低、色温统一偏暖。状态要的是「扫一眼分辨」，
  不是「抢注意力」。每种状态色配 `-soft`（浅底）与 `-ink`（浅底上的深字），
  深色主题下整体翻转。
- accent 只用于主操作、当前选中、状态指示、logo。**不做装饰**。
- 图表经 `--chart-1..6` 取色（赤陶橙为首的暖色序列）；SVG 的 fill/stroke 直接用
  `var()`，深浅主题自动适配，不再手工同步十六进制。

四层中性面表达层次，而不是靠边框：
`--canvas`（窗口底，米白） → `--surface`（内容面，纸白） → `--surface-2`（表头/分区）
→ `--surface-3`（侧栏/工具条） → `--surface-sunken`（输入框/进度槽）

## 层次：阴影 + 控件顶缘高光

卡片、面板、表格容器**一律无边框**，只有一层极轻的暖褐阴影（纸上叠纸）。
按钮有一线几乎不可见的顶缘内高光（`--btn-highlight`），按下时消失——
纸面控件的「立体感」全靠它。

## 排版

- 正文是系统无衬线栈（Segoe UI / 微软雅黑），承担标签、按钮、表格、正文。
- **展示衬线**（Georgia + 华文中宋/宋体，`--font-display`）只用于三处：
  页面标题（顶栏）、统计大数字、空态/英雄区标题。**小标题、按钮、表格数据
  永不用衬线**（product register：display 字体不进 UI 标签；小字号宋体也发虚）。
- **固定 px 档位，不用流体**：桌面 DPI 稳定，clamp 只会让侧栏里的标题变形。
- 数字一律 `font-variant-numeric: tabular-nums`：表格里的耗时、百分比刷新时列不该抖。

## 密度：紧凑默认，可切舒适

07 章要求桌面工具密度，核心场景是「一屏看完一批任务」。行高 32px（紧凑）/
40px（舒适），设置页切换（`<html data-density>`）。**紧凑体现在行高与留白，
不体现在视觉音量**——徽章、格式标签、行内按钮全部降噪。

## 动效

150–250ms，`cubic-bezier(0.16, 1, 0.3, 1)`（ease-out 指数曲线），无回弹。
**动效只传达状态**：hover 反馈、焦点、展开收起、切页淡入（170ms，
利用 `display:none` 重放动画的特性零成本实现）。

全局唯一的持续动画是「运行中」徽章的圆点呼吸。除此之外没有任何装饰性动效。

## 组件约定

每个交互组件都有完整状态：default / hover / focus-visible / active / disabled。
焦点环全局统一（`outline: 2px solid var(--brand)`）；输入框 focus 带 3px brand ring。

- **行内操作默认压到 55% 不透明度**，指针落到该行才浮出。
- **状态徽章带一个 6px 圆点**；轻提示（toast）用同一枚圆点表达状态——
  同一套视觉词汇，不发明第二种。
- **banner 用状态色的全边框 + 浅底**，不用彩色左边条。
- **文件格式标签保留 Office 各家色相**（浅底深字，深色主题翻转为暗底亮字）。
- 全局搜索框带 `Ctrl K` 快捷键提示（聚焦后隐藏），快捷键真实可用。
- 设置页字段按组装进「纸卡」（settings-card）；卡内不再嵌套自带阴影的容器。
- 侧栏顶部的赤陶橙 logo 方标是整个界面唯一的强色锚点，兼展开/收起开关。

## 明令避开

- side-stripe 彩色边（`border-left` 作强调）——banner/toast/切片卡/日志行全部改用
  全边框、底色 tint 或圆点
- 渐变文字（`background-clip: text`）
- 装饰性玻璃拟态
- hero-metric 模板：大数字 + 小标签 + **渐变点缀**。卖点指标只靠双倍宽 + 更大的数
- **等宽卡片平铺**：仪表盘的卖点指标（平均文本覆盖率）占双倍宽度
- 把 modal 当第一反应：详情一律用右侧抽屉，不打断队列视图
- 衬线字体出现在按钮、标签、表格数据上

## 视觉回归

```bash
cd app && npm run build && npx electron tools/ui-shot.mjs            # 浅色六页
cd app && npx electron tools/ui-shot.mjs .ui-shots-dark --theme=dark  # 深色六页
```

无头 Electron 加载构建产物 + 假 preload，六页各截一张到 `.ui-shots/`。
假数据刻意「有内容且不完美」——有成功也有警告和失败、名字有长有短、中英混排、
覆盖率有高有低。空页面看不出设计问题，全绿的页面同样看不出。
截图前会 `invalidate()` 强制重绘再等两帧：隐藏窗口的合成器不产新帧时，
capturePage 会端出上一页的旧帧（批量截图整体错位一页）。
