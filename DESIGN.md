# DESIGN.md —— DocFactory 设计系统

> 单一事实来源是 `app/src/renderer/src/styles.css` 的 `:root`。本文解释**为什么**是这些值。
> 与 07 章《UI 设计》配合：07 章定信息架构与文案，本文定视觉语言。

## register：product

design **serves** the product。用户在任务里，工具应当消失在任务背后。
成功标准不是「看起来像 AI 做不出来的」，而是：一个熟悉 Linear / Notion / Raycast 的人
坐下来就信任这个界面，不会在每个组件前停顿一下觉得哪里怪。

**失败模式不是平淡，是无目的的古怪**——过度装饰的按钮、样式不统一的表单控件、
给标准任务发明新控件。这里的标准是 earned familiarity。

## 主题：浅色（场景强制，非默认）

> 内网知识管理员，白天在办公室工位、1080p 屏前，把几百份历史文档丢进来批量整理，
> 一边等解析一边在三栏对照里核对表格有没有丢行。

这句话强制了浅色：文档本身是白底黑字，深色 UI 在「原文 vs Markdown」对照时造成
亮度跳变，长时间核对更累；用户也不是开发者，深色 IDE 审美对他们不构成加分。

## 色彩：Restrained + 单一色相贯穿

品牌色是**靛蓝**（OKLCH 色相 265），不是默认的 SaaS 蓝。更关键的是：
**所有中性色都带一点这个色相**（chroma 0.004–0.012）。整屏因此像一套东西，
而不是「灰底上放了个蓝按钮」。

- 没有 `#fff` / `#000`：纯白在大面积上发涩，纯黑文字在浅底上过硬。
- 状态色（绿/黄/红）刻意压低彩度。满饱和的状态色在一屏几十行里会互相打架，
  而状态要的是「扫一眼分辨」，不是「抢注意力」。
- accent 只用于主操作、当前选中、状态指示。**不做装饰**。

四层中性面表达层次，而不是靠边框：
`--canvas`（窗口底） → `--surface`（内容面） → `--surface-2`（表头/分区）
→ `--surface-3`（侧栏/工具条） → `--surface-sunken`（输入框/进度槽）

## 层次：阴影而非边框

卡片、面板、表格容器**一律无边框**，只有一层极轻的阴影。

原先每块都描一圈 1px 深灰边：一屏十几个盒子，等于把「这里是一个分区」这件事
讲了十几遍。阴影表达「浮起」，边框表达「圈禁」——这里要的是前者。
阴影颜色用品牌色相的暗色而非中性黑，投影带一点色温，看着不脏。

## 排版

- 系统字体栈（Segoe UI / 微软雅黑），一个字族承担全部角色。
- **固定 px 档位，不用流体**：桌面 DPI 稳定，clamp 只会让侧栏里的标题变形。
- 档位比例约 1.14，档多但每档差异明确（product 的 type 元素本来就多）。
- 数字一律 `font-variant-numeric: tabular-nums`：表格里的耗时、百分比刷新时列不该抖。

## 密度：紧凑，但不是拥挤

07 章要求桌面工具密度（非网页留白风），核心场景是「一屏看完一批任务」。
32px 行高、13px 正文。**紧凑体现在行高与留白，不体现在视觉音量**——
所以徽章、格式标签、行内按钮全部降噪，密度反而更可读。

## 动效

150–250ms，`cubic-bezier(0.16, 1, 0.3, 1)`（ease-out 指数曲线），无回弹。
**动效只传达状态**：hover 反馈、焦点、展开收起。

全局唯一的持续动画是「运行中」徽章的圆点呼吸——它回答了静态列表里最难判断的
问题：这行还在跑吗。除此之外没有任何装饰性动效。

## 组件约定

每个交互组件都有完整状态：default / hover / focus-visible / active / disabled。
焦点环全局统一（`outline: 2px solid var(--brand)`），不各写各的——
键盘用户在密集表格里最需要它。

**行内操作默认压到 55% 不透明度，指针落到该行才浮出**。
每行 4 个描边按钮 × 一屏几十行 = 上百个方框同时喊「点我」，
而用户 95% 的时间只是在扫列表。

**状态徽章带一个 6px 圆点**：让状态在余光里也能读出来，不必逐个看字。
纯标签类徽章（L0/L1/L2、格式名）没有圆点——它们不表达状态。

**文件格式标签保留 Office 各家色相**（用户确实靠颜色认格式），
但从「白字实心块」改为「浅底深字」：识别度不变，音量降一档，
文件名重新成为该行的主角。

## 明令避开

- side-stripe 彩色边（`border-left` 作强调）
- 渐变文字（`background-clip: text`）
- 装饰性玻璃拟态
- hero-metric 模板：大数字 + 小标签 + 渐变点缀
- **等宽卡片平铺**：仪表盘的卖点指标（平均文本覆盖率）占双倍宽度，
  等宽会让「内容完不完整」和「导了几个文件」看起来一样重要
- 把 modal 当第一反应：详情一律用右侧抽屉，不打断队列视图

## 视觉回归

```bash
cd app && npm run build && npx electron tools/ui-shot.mjs
```

无头 Electron 加载构建产物 + 假 preload，六页各截一张到 `.ui-shots/`。
假数据刻意「有内容且不完美」——有成功也有警告和失败、名字有长有短、中英混排、
覆盖率有高有低。空页面看不出设计问题，全绿的页面同样看不出。
