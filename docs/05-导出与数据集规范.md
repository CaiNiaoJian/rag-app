# 05 · 导出与数据集规范

## 1. 导出格式矩阵（六种）

| 格式 | 产物 | 说明 |
|---|---|---|
| Markdown | `{doc}.md` + `assets/` | 结构完整还原：标题层级、表格（MD 表；含合并单元格的复杂表用 HTML 表）、图片相对引用、脚注归尾 |
| PDF | `{doc}.pdf` | MD → 内置 HTML 模板 → Electron `printToPDF()`，**完全离线零外部依赖**；保留的是**逻辑结构**（标题/表格/图片），非原始版面复刻（文档中明示，避免预期偏差）；内嵌中文字体、专用打印 CSS（分页控制、表格跨页表头重复）、生成书签 |
| 切片 JSON | `{doc}.chunks.json` | 见 §2 |
| CSV | `{doc}.chunks.csv` | 切片扁平化，UTF-8 **BOM**（防 Excel 乱码） |
| Alpaca | `{name}.alpaca.json` | 微调数据集，见 §3 |
| ShareGPT | `{name}.sharegpt.json` | 微调数据集，见 §3 |

导出为异步任务（type=export）进队列；完成后提供「打开所在文件夹」。

## 2. 切片 JSON / CSV Schema

```json
{
  "schema_version": "1.0",
  "exported_by": "DocFactory 1.0.0",
  "docs": [{ "doc_id": "...", "doc_name": "合同v3.doc", "source_format": "doc",
             "parse_level": "L0", "text_coverage": 0.98 }],
  "chunks": [
    {
      "chunk_id": "c-0012", "parent_id": "p-0003", "doc_id": "...",
      "seq": 12, "kind": "child", "type": "text",
      "text": "……", "token_count": 486, "char_count": 812,
      "heading_path": "第2章>2.3 交付条款", "pages": [3, 4],
      "node_ids": ["n12", "n13"], "hash": "sha256:…"
    }
  ]
}
```

CSV 列（同字段扁平化）：`doc_name, chunk_id, parent_id, seq, kind, type, heading_path, pages, token_count, char_count, text`。

## 3. 微调数据集格式（契约 V1 冻结，覆盖 LLaMA-Factory / Axolotl 等主流框架）

### 3.1 Alpaca

```json
[
  {
    "instruction": "请根据以下内容回答：交付期限是多久？",
    "input": "（切片文本，可为空）",
    "output": "交付期限为合同签订后 90 天。",
    "metadata": { "doc_id": "...", "chunk_id": "c-0012", "heading_path": "第2章>2.3" }
  }
]
```

### 3.2 ShareGPT

```json
[
  { "conversations": [
      { "from": "human", "value": "交付期限是多久？\n\n参考内容：……" },
      { "from": "gpt", "value": "交付期限为合同签订后 90 天。" } ],
    "metadata": { "doc_id": "...", "chunk_id": "c-0012" } }
]
```

两者均提供 CSV 变体（列：instruction/input/output 或 human/gpt + metadata 列）。

### 3.3 V1 生成方式（用户二选一，UI 明示质量预期）

| 档位 | 行为 |
|---|---|
| **留空模板**（默认） | instruction 用通用模板 +切片文本填 input，output 留空——诚实产物，供人工标注或 V2 模型填充 |
| **规则生成**（实验性） | 基于标题/表格结构的规则生成简单 Q&A（如「X 章讲了什么」「表中 Y 的值」），标注 `"generated_by": "rule"`；文档与 UI 明示质量有限 |

V2 接入本地小模型后：仅替换生成器实现为 ModelProvider 调用（06 章），字段契约不变。

## 4. 导出中心交互（三栏，详见 07 章）

左：范围选择（单文件/勾选批量/「仅成功」快捷过滤）→ 中：格式选择 → 右：参数。

## 5. 处理选项面（FR-16，集中定义的全部用户可调项）

| 选项 | 位置 | 默认 |
|---|---|---|
| OCR：开 / 关（仅文本层）/ 高精度（需 server 模组） | 设置-通用 + 导入确认框 | 开 |
| 解析降级策略：自动降级 / 仅精解析（失败即报错） | 设置-高级 | 自动 |
| 单页解析超时 | 设置-高级 | 30s |
| 切片参数：target_tokens / overlap / 标题边界 / 表格原子 | 导出中心-右栏（触发重切） | 512 / 12% / 开 / 开 |
| 页眉页脚剔除、脚注归尾 | 导出中心-右栏 | 剔除 / 归尾 |
| PDF 导出：页眉页脚模板、字号 | 导出中心-右栏 | 内置模板 |
| 数据集：Alpaca/ShareGPT、JSON/CSV、留空模板/规则生成、每切片条数 | 导出中心-右栏 | Alpaca / JSON / 留空 / 1 |
| 并行任务数 | 设置-通用 | min(CPU/2, 4) |
| 输出目录 | 设置-通用 | workspace/exports |

原则：**主流程零配置**——所有默认值即最佳实践，专家参数收进「高级」折叠。
